% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KucoinWithdrawal.R
\name{KucoinWithdrawal}
\alias{KucoinWithdrawal}
\title{KucoinWithdrawal: Withdrawal Management}
\description{
KucoinWithdrawal: Withdrawal Management

KucoinWithdrawal: Withdrawal Management
}
\details{
Provides methods for creating, cancelling, and querying cryptocurrency
withdrawals on KuCoin. Inherits from \link{KucoinBase}.
\subsection{Purpose and Scope}{
\itemize{
\item \strong{Withdrawal Creation}: Initiate withdrawals to external addresses, KuCoin UIDs, email, or phone.
\item \strong{Withdrawal Cancellation}: Cancel pending withdrawals that are still in \code{PROCESSING} status.
\item \strong{Quota Queries}: Check withdrawal limits, minimum fees, and available balances per currency/chain.
\item \strong{History Retrieval}: Retrieve paginated withdrawal records with status tracking and timestamps.
\item \strong{Detail Lookup}: Get comprehensive details for a specific withdrawal by ID.
}
}

\subsection{Usage}{

All methods require authentication (valid API key, secret, passphrase).
The API key must have \strong{Withdrawal} permission for \code{add_withdrawal()} and
\code{cancel_withdrawal()}. Query methods (\verb{get_*}) require only \strong{General} permission.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Synchronous usage
withdrawal <- KucoinWithdrawal$new()
quotas <- withdrawal$get_withdrawal_quotas(currency = "BTC")

# Asynchronous usage
withdrawal_async <- KucoinWithdrawal$new(async = TRUE)
coro::async(function() \{
  quotas <- await(withdrawal_async$get_withdrawal_quotas(currency = "BTC"))
  print(quotas)
\})()
}\if{html}{\out{</div>}}
}

\subsection{Official Documentation}{

\href{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/withdraw-v3}{KuCoin Withdrawal Endpoints}
}

\subsection{Endpoints Covered}{\tabular{lll}{
   Method \tab Endpoint \tab HTTP \cr
   add_withdrawal \tab POST /api/v3/withdrawals \tab POST \cr
   cancel_withdrawal \tab DELETE /api/v1/withdrawals/\{withdrawalId\} \tab DELETE \cr
   get_withdrawal_quotas \tab GET /api/v1/withdrawals/quotas \tab GET \cr
   get_withdrawal_history \tab GET /api/v1/withdrawals \tab GET \cr
   get_withdrawal_by_id \tab GET /api/v1/withdrawals/\{withdrawalId\} \tab GET \cr
}

}
}
\examples{
\dontrun{
# Synchronous
withdrawal <- KucoinWithdrawal$new()
quotas <- withdrawal$get_withdrawal_quotas(currency = "BTC")
print(quotas)

# Asynchronous
withdrawal_async <- KucoinWithdrawal$new(async = TRUE)
main <- coro::async(function() {
  quotas <- await(withdrawal_async$get_withdrawal_quotas(currency = "BTC"))
  print(quotas)
})
main()
while (!later::loop_empty()) later::run_now()
}


## ------------------------------------------------
## Method `KucoinWithdrawal$add_withdrawal`
## ------------------------------------------------

\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Withdraw USDT via TRC20
result <- withdrawal$add_withdrawal(
  currency = "USDT",
  toAddress = "TKFRQXSDcY4kd3QLzw7uK16GmLrjJggwX8",
  amount = "10",
  withdrawType = "ADDRESS",
  chain = "trx"
)
print(result$withdrawal_id)

# Internal KuCoin transfer by UID
result <- withdrawal$add_withdrawal(
  currency = "BTC",
  toAddress = "12345678",
  amount = "0.01",
  withdrawType = "UID",
  isInner = TRUE
)
}

## ------------------------------------------------
## Method `KucoinWithdrawal$cancel_withdrawal`
## ------------------------------------------------

\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Cancel a pending withdrawal
result <- withdrawal$cancel_withdrawal("670deec84d64da0007d7c946")
print(result$withdrawal_id)
}

## ------------------------------------------------
## Method `KucoinWithdrawal$get_withdrawal_quotas`
## ------------------------------------------------

\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Check BTC withdrawal quotas
quotas <- withdrawal$get_withdrawal_quotas(currency = "BTC")
print(quotas[, .(currency, available_amount, withdraw_min_fee, is_withdraw_enabled)])

# Check USDT quotas on TRC20
usdt_quotas <- withdrawal$get_withdrawal_quotas(currency = "USDT", chain = "trx")
print(usdt_quotas$withdraw_min_fee)
}

## ------------------------------------------------
## Method `KucoinWithdrawal$get_withdrawal_history`
## ------------------------------------------------

\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Get all successful USDT withdrawals
history <- withdrawal$get_withdrawal_history(
  currency = "USDT",
  status = "SUCCESS"
)
print(history[, .(amount, status, created_at)])

# Get withdrawals from the last 24 hours
now_ms <- as.integer(as.numeric(lubridate::now()) * 1000)
recent <- withdrawal$get_withdrawal_history(
  currency = "BTC",
  startAt = now_ms - 86400000L,
  endAt = now_ms
)
}

## ------------------------------------------------
## Method `KucoinWithdrawal$get_withdrawal_by_id`
## ------------------------------------------------

\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Get withdrawal details
detail <- withdrawal$get_withdrawal_by_id("670deec84d64da0007d7c946")
print(detail[, .(id, currency, status, amount, cancel_type)])

# Check if a withdrawal can be cancelled
if (detail$cancel_type == "CANCELABLE") {
  withdrawal$cancel_withdrawal(detail$id)
}
}
}
\section{Super classes}{
\code{\link[connectcore:RestClient]{connectcore::RestClient}} -> \code{\link[kucoin:KucoinBase]{kucoin::KucoinBase}} -> \code{KucoinWithdrawal}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-KucoinWithdrawal-add_withdrawal}{\code{KucoinWithdrawal$add_withdrawal()}}
\item \href{#method-KucoinWithdrawal-cancel_withdrawal}{\code{KucoinWithdrawal$cancel_withdrawal()}}
\item \href{#method-KucoinWithdrawal-get_withdrawal_quotas}{\code{KucoinWithdrawal$get_withdrawal_quotas()}}
\item \href{#method-KucoinWithdrawal-get_withdrawal_history}{\code{KucoinWithdrawal$get_withdrawal_history()}}
\item \href{#method-KucoinWithdrawal-get_withdrawal_by_id}{\code{KucoinWithdrawal$get_withdrawal_by_id()}}
\item \href{#method-KucoinWithdrawal-clone}{\code{KucoinWithdrawal$clone()}}
}
}
\if{html}{\out{
<details open><summary>Inherited methods</summary>
<ul>
<li><span class="pkg-link" data-pkg="kucoin" data-topic="KucoinBase" data-id="initialize"><a href='../../kucoin/html/KucoinBase.html#method-KucoinBase-initialize'><code>kucoin::KucoinBase$initialize()</code></a></span></li>
</ul>
</details>
}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-add_withdrawal"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-add_withdrawal}{}}}
\subsection{Method \code{add_withdrawal()}}{
Add Withdrawal

Initiates a new withdrawal request. Supports withdrawals to external
blockchain addresses, KuCoin UIDs, email addresses, and phone numbers.
Only withdrawals in \code{PROCESSING} status can later be cancelled via
\code{cancel_withdrawal()}.
\subsection{Workflow}{
\enumerate{
\item \strong{Build Body}: Constructs JSON body with required and optional fields.
\item \strong{Request}: Authenticated POST to the withdrawal endpoint.
\item \strong{Parsing}: Returns \code{data.table} with the withdrawal ID.
}
}

\subsection{API Endpoint}{

\verb{POST https://api.kucoin.com/api/v3/withdrawals}
}

\subsection{Official Documentation}{

\href{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/withdraw-v3}{KuCoin Withdraw V3}

Verified: 2026-05-23
}

\subsection{Automated Trading Usage}{
\itemize{
\item \strong{Profit Extraction}: Withdraw profits to a cold wallet at regular intervals.
\item \strong{Arbitrage Settlement}: Move funds off-exchange after capturing arbitrage spreads.
\item \strong{Internal Transfers}: Use \code{isInner = TRUE} for fee-free transfers between KuCoin accounts.
\item \strong{Multi-Chain Support}: Specify \code{chain} (e.g., \code{"trx"}, \code{"eth"}, \code{"bsc"}) to select the cheapest or fastest
network.
}
}

\subsection{curl}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request POST 'https://api.kucoin.com/api/v3/withdrawals' \\
  --header 'Content-Type: application/json' \\
  --header 'KC-API-KEY: your-api-key' \\
  --header 'KC-API-SIGN: your-signature' \\
  --header 'KC-API-TIMESTAMP: 1729176273859' \\
  --header 'KC-API-PASSPHRASE: your-passphrase' \\
  --header 'KC-API-KEY-VERSION: 2' \\
  --data-raw \\
  '\{"currency":"USDT","toAddress":"TKFRQXSDcY4kd3QLzw7uK16GmLrjJggwX8","amount":"10","withdrawType":"ADDRESS",
  "chain":"trx"\}'
}\if{html}{\out{</div>}}
}

\subsection{JSON Response}{

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "code": "200000",
  "data": \{
    "withdrawalId": "670deec84d64da0007d7c946"
  \}
\}
}\if{html}{\out{</div>}}
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$add_withdrawal(
  currency,
  toAddress,
  amount,
  withdrawType,
  chain = NULL,
  memo = NULL,
  isInner = NULL,
  remark = NULL,
  feeDeductType = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) currency code (e.g., \code{"BTC"},
\code{"USDT"}).}

\item{\code{toAddress}}{(scalar<character>) withdrawal destination address, UID,
email, or phone number.}

\item{\code{amount}}{(scalar<character>) withdrawal amount (must be positive,
multiple of currency precision).}

\item{\code{withdrawType}}{(scalar<character>) withdrawal type: \code{"ADDRESS"},
\code{"UID"}, \code{"MAIL"}, or \code{"PHONE"}.}

\item{\code{chain}}{(scalar<character> | NULL) blockchain network identifier
(e.g., \code{"eth"}, \code{"trx"}, \code{"bsc"}). Required by the KuCoin API; the method
raises if \code{NULL}.}

\item{\code{memo}}{(scalar<character> | NULL) address memo/tag (required for some
currencies like XRP, XLM).}

\item{\code{isInner}}{(scalar<logical> | NULL) if \code{TRUE}, this is an internal
KuCoin transfer (no on-chain fee).}

\item{\code{remark}}{(scalar<character> | NULL) optional remark for the
withdrawal.}

\item{\code{feeDeductType}}{(scalar<character> | NULL) fee deduction type:
\code{"INTERNAL"} or \code{"EXTERNAL"}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(data.table | promise<data.table>) one row with column
\code{withdrawal_id} (character): the unique withdrawal identifier:
\itemize{
\item withdrawal_id (character) the withdrawal id.
}
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Withdraw USDT via TRC20
result <- withdrawal$add_withdrawal(
  currency = "USDT",
  toAddress = "TKFRQXSDcY4kd3QLzw7uK16GmLrjJggwX8",
  amount = "10",
  withdrawType = "ADDRESS",
  chain = "trx"
)
print(result$withdrawal_id)

# Internal KuCoin transfer by UID
result <- withdrawal$add_withdrawal(
  currency = "BTC",
  toAddress = "12345678",
  amount = "0.01",
  withdrawType = "UID",
  isInner = TRUE
)
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-cancel_withdrawal"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-cancel_withdrawal}{}}}
\subsection{Method \code{cancel_withdrawal()}}{
Cancel Withdrawal

Cancels a pending withdrawal request. Only withdrawals with \code{PROCESSING}
status can be cancelled. Once a withdrawal has moved to \code{WALLET_PROCESSING}
or later, it cannot be reversed.
\subsection{Workflow}{
\enumerate{
\item \strong{Request}: Authenticated DELETE to the withdrawal-specific endpoint.
\item \strong{Response}: KuCoin returns \code{NULL} data on success.
\item \strong{Parsing}: Returns a \code{data.table} with the cancelled withdrawal ID.
}
}

\subsection{API Endpoint}{

\verb{DELETE https://api.kucoin.com/api/v1/withdrawals/\{withdrawalId\}}
}

\subsection{Official Documentation}{

\href{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/cancel-withdrawal}{KuCoin Cancel Withdrawal}

Verified: 2026-05-23
}

\subsection{Automated Trading Usage}{
\itemize{
\item \strong{Error Recovery}: Cancel a withdrawal if the destination address was incorrect.
\item \strong{Strategy Change}: Cancel pending withdrawals if market conditions change and funds are needed for trading.
}
}

\subsection{curl}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request DELETE \\
  'https://api.kucoin.com/api/v1/withdrawals/670deec84d64da0007d7c946' \\
  --header 'KC-API-KEY: your-api-key' \\
  --header 'KC-API-SIGN: your-signature' \\
  --header 'KC-API-TIMESTAMP: 1729176273859' \\
  --header 'KC-API-PASSPHRASE: your-passphrase' \\
  --header 'KC-API-KEY-VERSION: 2'
}\if{html}{\out{</div>}}
}

\subsection{JSON Response}{

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "code": "200000",
  "data": null
\}
}\if{html}{\out{</div>}}
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$cancel_withdrawal(withdrawalId)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{withdrawalId}}{(scalar<character>) the unique withdrawal ID to
cancel.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(data.table | promise<data.table>) one row, the cancelled
withdrawal ID echoed from the input (KuCoin returns \code{null} data on a
successful cancel):
\itemize{
\item withdrawal_id (character) the cancelled withdrawal ID:
\item withdrawal_id (character) the withdrawal id.
}
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Cancel a pending withdrawal
result <- withdrawal$cancel_withdrawal("670deec84d64da0007d7c946")
print(result$withdrawal_id)
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-get_withdrawal_quotas"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-get_withdrawal_quotas}{}}}
\subsection{Method \code{get_withdrawal_quotas()}}{
Get Withdrawal Quotas

Retrieves withdrawal limits, minimum fees, and available balances for a
currency. Essential for pre-flight checks before initiating withdrawals
to ensure sufficient balance and valid amount parameters.
\subsection{Workflow}{
\enumerate{
\item \strong{Request}: Authenticated GET with \code{currency} (required) and optional \code{chain} query parameters.
\item \strong{Parsing}: Returns a \code{data.table} with quota details, limits, and fee information.
}
}

\subsection{API Endpoint}{

\verb{GET https://api.kucoin.com/api/v1/withdrawals/quotas}
}

\subsection{Official Documentation}{

KuCoin Get Withdrawal Quotas:
\url{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/get-withdrawal-quotas}

Verified: 2026-05-23
}

\subsection{Automated Trading Usage}{
\itemize{
\item \strong{Pre-Flight Check}: Verify \code{is_withdraw_enabled} and \code{available_amount} before attempting a withdrawal.
\item \strong{Fee Estimation}: Use \code{withdraw_min_fee} and \code{inner_withdraw_min_fee} for cost calculations.
\item \strong{Amount Validation}: Check \code{withdraw_min_size} and \code{precision} to format withdrawal amounts correctly.
\item \strong{Limit Awareness}: Monitor \code{remain_amount} against \code{limit_btc_amount} to stay within daily limits.
}
}

\subsection{curl}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request GET \\
  'https://api.kucoin.com/api/v1/withdrawals/quotas?currency=BTC' \\
  --header 'KC-API-KEY: your-api-key' \\
  --header 'KC-API-SIGN: your-signature' \\
  --header 'KC-API-TIMESTAMP: 1729176273859' \\
  --header 'KC-API-PASSPHRASE: your-passphrase' \\
  --header 'KC-API-KEY-VERSION: 2'
}\if{html}{\out{</div>}}
}

\subsection{JSON Response}{

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "code": "200000",
  "data": \{
    "currency": "BTC",
    "limitBTCAmount": "15.79590095",
    "usedBTCAmount": "0.00000000",
    "quotaCurrency": "USDT",
    "limitQuotaCurrencyAmount": "999999.00000000",
    "usedQuotaCurrencyAmount": "0",
    "remainAmount": "15.79590095",
    "availableAmount": "0",
    "withdrawMinFee": "0.0005",
    "innerWithdrawMinFee": "0",
    "withdrawMinSize": "0.001",
    "isWithdrawEnabled": true,
    "precision": 8,
    "chain": "BTC",
    "reason": null,
    "lockedAmount": "0"
  \}
\}
}\if{html}{\out{</div>}}
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$get_withdrawal_quotas(currency, chain = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character>) currency code (e.g., \code{"BTC"},
\code{"USDT"}).}

\item{\code{chain}}{(scalar<character> | NULL) blockchain network identifier
(e.g., \code{"eth"}, \code{"trx"}). When NULL, returns quotas for the default
chain.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(data.table | promise<data.table>) one row with the withdrawal
quota details (currency, limit_btc_amount, used_btc_amount,
quota_currency, limit_quota_currency_amount, used_quota_currency_amount,
remain_amount, available_amount, withdraw_min_fee, inner_withdraw_min_fee,
withdraw_min_size, is_withdraw_enabled, precision, chain, reason,
locked_amount, ...):
\itemize{
\item currency (character) the currency code.
\item chain (character) the chain code.
\item is_withdraw_enabled (logical | NA) the is withdraw enabled.
\item available_amount (character | NA) the available amount.
\item remain_amount (character | NA) the remain amount.
\item withdraw_min_fee (character | NA) the withdraw min fee.
\item inner_withdraw_min_fee (character | NA) the inner withdraw min fee.
\item withdraw_min_size (character | NA) the withdraw min size.
\item precision (integer | NA) the decimal precision.
\item limit_btc_amount (character | NA) the limit btc amount.
\item used_btc_amount (character | NA) the used btc amount.
\item locked_amount (character | NA) the locked amount.
\item quota_currency (character) the quota currency.
\item limit_quota_currency_amount (character | NA) the limit quota currency amount.
\item used_quota_currency_amount (character | NA) the used quota currency amount.
\item reason (logical | NA) the reason, when present.
}
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Check BTC withdrawal quotas
quotas <- withdrawal$get_withdrawal_quotas(currency = "BTC")
print(quotas[, .(currency, available_amount, withdraw_min_fee, is_withdraw_enabled)])

# Check USDT quotas on TRC20
usdt_quotas <- withdrawal$get_withdrawal_quotas(currency = "USDT", chain = "trx")
print(usdt_quotas$withdraw_min_fee)
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-get_withdrawal_history"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-get_withdrawal_history}{}}}
\subsection{Method \code{get_withdrawal_history()}}{
Get Withdrawal History

Retrieves paginated withdrawal history with optional filtering by currency,
status, and time range. Automatically converts \code{created_at} timestamps
to POSIXct for convenient analysis.
\subsection{Workflow}{
\enumerate{
\item \strong{Pagination}: Uses \code{private$.paginate()} to fetch all pages of withdrawal records up to \code{max_pages}.
\item \strong{Flattening}: Combines all pages into a single \code{data.table} via \code{flatten_pages()}.
\item \strong{Timestamp Conversion}: Coerces \code{created_at} (milliseconds) to POSIXct in-place.
}
}

\subsection{API Endpoint}{

\verb{GET https://api.kucoin.com/api/v1/withdrawals}
}

\subsection{Official Documentation}{

KuCoin Get Withdrawal History:
\url{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/get-withdrawal-history}

Verified: 2026-05-23
}

\subsection{Automated Trading Usage}{
\itemize{
\item \strong{Withdrawal Monitoring}: Poll for \code{"SUCCESS"} status to confirm funds have left the exchange.
\item \strong{Reconciliation}: Match \code{wallet_tx_id} against on-chain transaction hashes for audit.
\item \strong{Time-Windowed Queries}: Use \code{startAt}/\code{endAt} timestamps to retrieve withdrawals within a specific period.
}
}

\subsection{curl}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request GET \\
  'https://api.kucoin.com/api/v1/withdrawals?currency=USDT&status=SUCCESS&currentPage=1&pageSize=50' \\
  --header 'KC-API-KEY: your-api-key' \\
  --header 'KC-API-SIGN: your-signature' \\
  --header 'KC-API-TIMESTAMP: 1729176273859' \\
  --header 'KC-API-PASSPHRASE: your-passphrase' \\
  --header 'KC-API-KEY-VERSION: 2'
}\if{html}{\out{</div>}}
}

\subsection{JSON Response}{

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "code": "200000",
  "data": \{
    "currentPage": 1,
    "pageSize": 50,
    "totalNum": 1,
    "totalPage": 1,
    "items": [
      \{
        "currency": "USDT",
        "chain": "",
        "status": "SUCCESS",
        "address": "a435*****@gmail.com",
        "memo": "",
        "isInner": true,
        "amount": "1.00000000",
        "fee": "0.00000000",
        "walletTxId": null,
        "createdAt": 1728555875000,
        "updatedAt": 1728555875000,
        "remark": "",
        "arrears": false
      \}
    ]
  \}
\}
}\if{html}{\out{</div>}}
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$get_withdrawal_history(
  currency = NULL,
  status = NULL,
  startAt = NULL,
  endAt = NULL,
  page_size = 50,
  max_pages = Inf
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{currency}}{(scalar<character> | NULL) currency code (e.g., \code{"BTC"},
\code{"USDT"}). If NULL, returns withdrawals for all currencies.}

\item{\code{status}}{(scalar<character> | NULL) filter by withdrawal status.
Accepted values: \code{"PROCESSING"}, \code{"REVIEW"}, \code{"WALLET_PROCESSING"},
\code{"SUCCESS"}, \code{"FAILURE"}. When NULL, returns withdrawals of all statuses.}

\item{\code{startAt}}{(scalar<numeric> | NULL) start timestamp in milliseconds
(inclusive).}

\item{\code{endAt}}{(scalar<numeric> | NULL) end timestamp in milliseconds
(inclusive).}

\item{\code{page_size}}{(scalar<count in [1, Inf[>) number of results per page
(default 50, max 500).}

\item{\code{max_pages}}{(scalar<numeric in [1, Inf]>) maximum number of pages to
fetch (default \code{Inf} for all pages).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(data.table | promise<data.table>) one row per withdrawal record
(currency, chain, status, address, memo, is_inner, amount, fee,
wallet_tx_id, created_at, updated_at, remark, arrears, ...), with
\code{created_at}/\code{updated_at} coerced to POSIXct, or an empty \code{data.table}
if no withdrawals match the filters.
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Get all successful USDT withdrawals
history <- withdrawal$get_withdrawal_history(
  currency = "USDT",
  status = "SUCCESS"
)
print(history[, .(amount, status, created_at)])

# Get withdrawals from the last 24 hours
now_ms <- as.integer(as.numeric(lubridate::now()) * 1000)
recent <- withdrawal$get_withdrawal_history(
  currency = "BTC",
  startAt = now_ms - 86400000L,
  endAt = now_ms
)
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-get_withdrawal_by_id"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-get_withdrawal_by_id}{}}}
\subsection{Method \code{get_withdrawal_by_id()}}{
Get Withdrawal by ID

Retrieves comprehensive details for a specific withdrawal, including
chain information, failure reasons, cancel status, and return details.
Provides more information than the history endpoint.
\subsection{Workflow}{
\enumerate{
\item \strong{Request}: Authenticated GET to the withdrawal-specific endpoint.
\item \strong{Response}: KuCoin returns detailed withdrawal information.
\item \strong{Parsing}: Returns \code{data.table} with full withdrawal details.
}
}

\subsection{API Endpoint}{

\verb{GET https://api.kucoin.com/api/v1/withdrawals/\{withdrawalId\}}
}

\subsection{Official Documentation}{

KuCoin Get Withdrawal Detail:
\url{https://www.kucoin.com/docs-new/rest/account-info/withdrawals/get-withdrawal-by-id}

Verified: 2026-05-23
}

\subsection{Automated Trading Usage}{
\itemize{
\item \strong{Status Tracking}: Monitor withdrawal progress through \code{REVIEW} → \code{PROCESSING} → \code{WALLET_PROCESSING} →
\code{SUCCESS}.
\item \strong{Failure Diagnosis}: Check \code{failure_reason} and \code{failure_reason_msg} to understand why a withdrawal failed.
\item \strong{Cancel Eligibility}: Use \code{cancel_type} (\code{"CANCELABLE"}, \code{"CANCELING"}, \code{"NON_CANCELABLE"}) to determine if a
withdrawal can still be cancelled.
}
}

\subsection{curl}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{curl --location --request GET \\
  'https://api.kucoin.com/api/v1/withdrawals/670deec84d64da0007d7c946' \\
  --header 'KC-API-KEY: your-api-key' \\
  --header 'KC-API-SIGN: your-signature' \\
  --header 'KC-API-TIMESTAMP: 1729176273859' \\
  --header 'KC-API-PASSPHRASE: your-passphrase' \\
  --header 'KC-API-KEY-VERSION: 2'
}\if{html}{\out{</div>}}
}

\subsection{JSON Response}{

\if{html}{\out{<div class="sourceCode json">}}\preformatted{\{
  "code": "200000",
  "data": \{
    "id": "67e6515f7960ba0007b42025",
    "currency": "USDT",
    "chainId": "trx",
    "chainName": "TRC20",
    "status": "SUCCESS",
    "address": "TKFRQXSDcY4kd3QLzw7uK16GmLrjJggwX8",
    "memo": "",
    "isInner": true,
    "amount": "3.00000000",
    "fee": "0.00000000",
    "walletTxId": null,
    "createdAt": 1743147359000,
    "cancelType": "NON_CANCELABLE"
  \}
\}
}\if{html}{\out{</div>}}
}
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$get_withdrawal_by_id(withdrawalId)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{withdrawalId}}{(scalar<character>) the unique withdrawal ID.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
(data.table | promise<data.table>) one row with the full
withdrawal detail (id, currency, chain_id, chain_name, status, address,
memo, is_inner, amount, fee, wallet_tx_id, cancel_type, failure_reason,
failure_reason_msg, created_at, ...), with \code{created_at} coerced to
POSIXct:
\itemize{
\item id (character) the record identifier.
\item currency (character) the currency code.
\item chain_id (character | NA) the chain identifier.
\item chain_name (character | NA) the chain name.
\item status (character) the status.
\item address (character) the address.
\item memo (character) the address memo/tag.
\item is_inner (logical) the is inner.
\item amount (character | NA) the amount.
\item fee (character | NA) the fee.
\item wallet_tx_id (logical | NA) the wallet tx id.
\item created_at (POSIXct) the created at (UTC).
\item cancel_type (character) the cancel type.
\item uid (integer) the user identifier.
\item currency_name (character) the currency name.
\item failure_reason (character | NA) the failure reason.
\item failure_reason_msg (logical | NA) the failure reason msg.
\item address_remark (character) the address remark.
\item remark (character | NA) an optional remark.
\item taxes (logical | NA) the taxes.
\item tax_description (logical | NA) the tax description.
\item tx_id (logical | NA) the tx id.
\item return_status (character) the return status.
\item return_amount (logical | NA) the return amount.
\item return_currency (character) the return currency.
}
}
\subsection{Examples}{
\if{html}{\out{<div class="r example copy">}}
\preformatted{\dontrun{
withdrawal <- KucoinWithdrawal$new()

# Get withdrawal details
detail <- withdrawal$get_withdrawal_by_id("670deec84d64da0007d7c946")
print(detail[, .(id, currency, status, amount, cancel_type)])

# Check if a withdrawal can be cancelled
if (detail$cancel_type == "CANCELABLE") {
  withdrawal$cancel_withdrawal(detail$id)
}
}
}
\if{html}{\out{</div>}}

}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-KucoinWithdrawal-clone"></a>}}
\if{latex}{\out{\hypertarget{method-KucoinWithdrawal-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{KucoinWithdrawal$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
