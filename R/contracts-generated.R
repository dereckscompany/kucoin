# Generated by roxyassert: do not edit by hand.
# Re-generated on devtools::document(). Commit this file like NAMESPACE.

assert_return_KucoinAccount__get_summary <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("level", "sub_quantity", "max_default_sub_quantity", "max_sub_quantity", "spot_sub_quantity", "margin_sub_quantity", "futures_sub_quantity", "option_sub_quantity", "max_spot_sub_quantity", "max_margin_sub_quantity", "max_futures_sub_quantity", "max_option_sub_quantity"))
  assert_integer(value[["level"]])
  assert_integer(value[["sub_quantity"]])
  assert_integer(value[["max_default_sub_quantity"]])
  assert_integer(value[["max_sub_quantity"]])
  assert_integer(value[["spot_sub_quantity"]])
  assert_integer(value[["margin_sub_quantity"]])
  assert_integer(value[["futures_sub_quantity"]])
  assert_integer(value[["option_sub_quantity"]])
  assert_integer(value[["max_spot_sub_quantity"]])
  assert_integer(value[["max_margin_sub_quantity"]])
  assert_integer(value[["max_futures_sub_quantity"]])
  assert_integer(value[["max_option_sub_quantity"]])
  return(value)
}

assert_return_KucoinAccount__get_apikey_info <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("remark", "api_key", "api_version", "permission", "created_at", "uid", "is_master", "region", "kyc_status", "site_type"))
  assert_character(value[["remark"]])
  assert_character(value[["api_key"]])
  assert_integer(value[["api_version"]])
  assert_character(value[["permission"]])
  assert_no_missing_values(value[["permission"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_integer(value[["uid"]])
  assert_no_missing_values(value[["uid"]])
  assert_logical(value[["is_master"]])
  assert_no_missing_values(value[["is_master"]])
  assert_character(value[["region"]])
  assert_character(value[["kyc_status"]])
  assert_character(value[["site_type"]])
  return(value)
}

assert_return_KucoinAccount__get_spot_account_type <- function(value) {
  assert_scalar_logical(value)
  return(value)
}

assert_args_KucoinAccount__get_spot_accounts <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_spot_accounts <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "currency", "type", "balance", "available", "holds"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_double(value[["balance"]])
  assert_double(value[["available"]])
  assert_double(value[["holds"]])
  return(value)
}

assert_args_KucoinAccount__get_spot_account_detail <- function(account_id) {
  assert_scalar_character(account_id)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_spot_account_detail <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "balance", "available", "holds"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["balance"]])
  assert_double(value[["available"]])
  assert_double(value[["holds"]])
  return(value)
}

assert_args_KucoinAccount__get_cross_margin_account <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_cross_margin_account <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinAccount__get_isolated_margin_account <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_isolated_margin_account <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinAccount__get_spot_ledger <- function(query, page_size, max_pages) {
  assert_list(query)
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_spot_ledger <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "currency", "amount", "fee", "balance", "account_type", "biz_type", "direction", "context", "created_at"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["amount"]])
  assert_double(value[["fee"]])
  assert_double(value[["balance"]])
  assert_character(value[["account_type"]])
  assert_no_missing_values(value[["account_type"]])
  assert_character(value[["biz_type"]])
  assert_no_missing_values(value[["biz_type"]])
  assert_character(value[["direction"]])
  assert_no_missing_values(value[["direction"]])
  assert_character(value[["context"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  return(value)
}

assert_args_KucoinAccount__get_hf_ledger <- function(currency, direction, biz_type, last_id, limit, start_at, end_at) {
  if (!is.null(currency)) {
    assert_scalar_character(currency)
  }
  if (!is.null(direction)) {
    assert_scalar_character(direction)
  }
  if (!is.null(biz_type)) {
    assert_scalar_character(biz_type)
  }
  if (!is.null(last_id)) {
    assert_scalar_character(last_id)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(start_at)) {
    assert_scalar_double(start_at)
  }
  if (!is.null(end_at)) {
    assert_scalar_double(end_at)
  }
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_hf_ledger <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "currency", "amount", "fee", "tax", "balance", "account_type", "biz_type", "direction", "context", "created_at"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["amount"]])
  assert_double(value[["fee"]])
  assert_double(value[["tax"]])
  assert_double(value[["balance"]])
  assert_character(value[["account_type"]])
  assert_no_missing_values(value[["account_type"]])
  assert_character(value[["biz_type"]])
  assert_no_missing_values(value[["biz_type"]])
  assert_character(value[["direction"]])
  assert_no_missing_values(value[["direction"]])
  assert_character(value[["context"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  return(value)
}

assert_args_KucoinAccount__get_base_fee_rate <- function(currency_type) {
  if (!is.null(currency_type)) {
    assert_scalar_count(currency_type)
  }
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_base_fee_rate <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("taker_fee_rate", "maker_fee_rate"))
  assert_double(value[["taker_fee_rate"]])
  assert_double(value[["maker_fee_rate"]])
  return(value)
}

assert_args_KucoinAccount__get_fee_rate <- function(symbols) {
  assert_scalar_character(symbols)
  return(invisible(NULL))
}

assert_return_KucoinAccount__get_fee_rate <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "taker_fee_rate", "maker_fee_rate"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["taker_fee_rate"]])
  assert_double(value[["maker_fee_rate"]])
  return(value)
}

assert_args_KucoinBase__initialize <- function(keys, base_url, async) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  return(invisible(NULL))
}

assert_return_KucoinBase__initialize <- function(value) {
  assert_class(value, "KucoinBase")
  return(value)
}

assert_args_KucoinDeposit__add_deposit_address <- function(currency, chain, to, amount) {
  assert_scalar_character(currency)
  if (!is.null(chain)) {
    assert_scalar_character(chain)
  }
  if (!is.null(to)) {
    assert_scalar_character(to)
  }
  if (!is.null(amount)) {
    assert_scalar_character(amount)
  }
  return(invisible(NULL))
}

assert_return_KucoinDeposit__add_deposit_address <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("address", "memo", "chain", "chain_id", "to", "currency", "contract_address"))
  assert_character(value[["address"]])
  assert_no_missing_values(value[["address"]])
  assert_character(value[["memo"]])
  assert_no_missing_values(value[["memo"]])
  assert_character(value[["chain"]])
  assert_no_missing_values(value[["chain"]])
  assert_character(value[["chain_id"]])
  assert_character(value[["to"]])
  assert_no_missing_values(value[["to"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_character(value[["contract_address"]])
  return(value)
}

assert_args_KucoinDeposit__get_deposit_addresses <- function(currency, amount, chain) {
  assert_scalar_character(currency)
  if (!is.null(amount)) {
    assert_scalar_character(amount)
  }
  if (!is.null(chain)) {
    assert_scalar_character(chain)
  }
  return(invisible(NULL))
}

assert_return_KucoinDeposit__get_deposit_addresses <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinDeposit__get_deposit_history <- function(currency, status, start_at, end_at, page_size, max_pages) {
  if (!is.null(currency)) {
    assert_scalar_character(currency)
  }
  if (!is.null(status)) {
    assert_scalar_character(status)
  }
  if (!is.null(start_at)) {
    assert_scalar_double(start_at)
  }
  if (!is.null(end_at)) {
    assert_scalar_double(end_at)
  }
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinDeposit__get_deposit_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__initialize <- function(keys, base_url, async) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__initialize <- function(value) {
  assert_class(value, "KucoinFuturesAccount")
  return(value)
}

assert_args_KucoinFuturesAccount__get_account_overview <- function(currency) {
  assert_scalar_character(currency)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_account_overview <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("account_equity", "unrealised_pnl", "margin_balance", "available_balance", "available_margin", "currency", "risk_ratio", "max_withdraw_amount"))
  assert_double(value[["account_equity"]])
  assert_double(value[["unrealised_pnl"]])
  assert_double(value[["margin_balance"]])
  assert_double(value[["available_balance"]])
  assert_double(value[["available_margin"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["risk_ratio"]])
  assert_double(value[["max_withdraw_amount"]])
  return(value)
}

assert_args_KucoinFuturesAccount__get_position <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_position <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__get_positions <- function(currency) {
  if (!is.null(currency)) {
    assert_scalar_character(currency)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_positions <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__get_positions_history <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_positions_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__get_margin_mode <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_margin_mode <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "margin_mode"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  return(value)
}

assert_args_KucoinFuturesAccount__set_margin_mode <- function(symbol, margin_mode) {
  assert_scalar_character(symbol)
  assert_scalar_character(margin_mode)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__set_margin_mode <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "margin_mode"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  return(value)
}

assert_args_KucoinFuturesAccount__get_cross_margin_leverage <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_cross_margin_leverage <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "leverage"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["leverage"]])
  return(value)
}

assert_args_KucoinFuturesAccount__set_cross_margin_leverage <- function(symbol, leverage) {
  assert_scalar_character(symbol)
  assert_scalar_count(leverage)
  assert_between(leverage, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__set_cross_margin_leverage <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "leverage"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["leverage"]])
  return(value)
}

assert_args_KucoinFuturesAccount__get_max_open_size <- function(symbol, price, leverage) {
  assert_scalar_character(symbol)
  assert_scalar_double(price)
  assert_between(price, lower = 0, lower_inclusive = FALSE, upper = Inf, upper_inclusive = FALSE)
  assert_scalar_count(leverage)
  assert_between(leverage, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_max_open_size <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "max_buy_open_size", "max_sell_open_size"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_integer(value[["max_buy_open_size"]])
  assert_integer(value[["max_sell_open_size"]])
  return(value)
}

assert_args_KucoinFuturesAccount__get_max_withdraw_margin <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_max_withdraw_margin <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__add_isolated_margin <- function(symbol, margin, biz_no) {
  assert_scalar_character(symbol)
  assert_scalar_double(margin)
  assert_scalar_character(biz_no)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__add_isolated_margin <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "margin", "margin_type"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["margin"]])
  assert_character(value[["margin_type"]])
  assert_no_missing_values(value[["margin_type"]])
  return(value)
}

assert_args_KucoinFuturesAccount__remove_isolated_margin <- function(symbol, withdraw_amount) {
  assert_scalar_character(symbol)
  assert_scalar_double(withdraw_amount)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__remove_isolated_margin <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "margin", "margin_type"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["margin"]])
  assert_character(value[["margin_type"]])
  assert_no_missing_values(value[["margin_type"]])
  return(value)
}

assert_args_KucoinFuturesAccount__get_risk_limit <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_risk_limit <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesAccount__get_funding_history <- function(symbol, query) {
  assert_scalar_character(symbol)
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinFuturesAccount__get_funding_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesMarketData__initialize <- function(keys, base_url, async) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__initialize <- function(value) {
  assert_class(value, "KucoinFuturesMarketData")
  return(value)
}

assert_args_KucoinFuturesMarketData__get_contract <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_contract <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinFuturesMarketData__get_all_contracts <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesMarketData__get_ticker <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("sequence", "symbol", "side", "size", "price", "best_bid_size", "best_bid_price", "best_ask_price", "best_ask_size", "trade_id", "ts"))
  assert_double(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["size"]])
  assert_double(value[["price"]])
  assert_double(value[["best_bid_size"]])
  assert_double(value[["best_bid_price"]])
  assert_double(value[["best_ask_price"]])
  assert_double(value[["best_ask_size"]])
  assert_character(value[["trade_id"]])
  assert_no_missing_values(value[["trade_id"]])
  assert_datetime(value[["ts"]])
  assert_no_missing_values(value[["ts"]])
  return(value)
}

assert_return_KucoinFuturesMarketData__get_all_tickers <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesMarketData__get_part_orderbook <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_part_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("ts", "sequence", "side", "level", "price", "size", "symbol"))
  assert_datetime(value[["ts"]])
  assert_no_missing_values(value[["ts"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_integer(value[["level"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  return(value)
}

assert_args_KucoinFuturesMarketData__get_full_orderbook <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_full_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("ts", "sequence", "side", "level", "price", "size", "symbol"))
  assert_datetime(value[["ts"]])
  assert_no_missing_values(value[["ts"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_integer(value[["level"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  return(value)
}

assert_args_KucoinFuturesMarketData__get_trade_history <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_trade_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesMarketData__get_klines <- function(symbol, granularity, fetch_all, sleep) {
  assert_scalar_character(symbol)
  assert_scalar_count(granularity)
  assert_between(granularity, lower = 1)
  assert_scalar_logical(fetch_all)
  assert_scalar_double(sleep)
  assert_between(sleep, lower = 0, upper = Inf, upper_inclusive = FALSE)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "turnover"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["close"]])
  assert_double(value[["volume"]])
  assert_double(value[["turnover"]])
  return(value)
}

assert_args_KucoinFuturesMarketData__get_mark_price <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_mark_price <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "granularity", "time_point", "value", "index_price"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_integer(value[["granularity"]])
  assert_datetime(value[["time_point"]])
  assert_no_missing_values(value[["time_point"]])
  assert_double(value[["value"]])
  assert_double(value[["index_price"]])
  return(value)
}

assert_args_KucoinFuturesMarketData__get_funding_rate <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_funding_rate <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "granularity", "time_point", "value", "daily_interest_rate", "funding_rate_cap", "funding_rate_floor", "period", "funding_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_integer(value[["granularity"]])
  assert_datetime(value[["time_point"]])
  assert_no_missing_values(value[["time_point"]])
  assert_double(value[["value"]])
  assert_double(value[["daily_interest_rate"]])
  assert_double(value[["funding_rate_cap"]])
  assert_double(value[["funding_rate_floor"]])
  assert_integer(value[["period"]])
  assert_datetime(value[["funding_time"]])
  assert_no_missing_values(value[["funding_time"]])
  return(value)
}

assert_args_KucoinFuturesMarketData__get_funding_history <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesMarketData__get_funding_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinFuturesMarketData__get_server_time <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("server_time"))
  assert_datetime(value[["server_time"]])
  assert_no_missing_values(value[["server_time"]])
  return(value)
}

assert_return_KucoinFuturesMarketData__get_service_status <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("status", "msg"))
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  return(value)
}

assert_args_parse_futures_orderbook <- function(data) {
  assert_list(data)
  return(invisible(NULL))
}

assert_return_parse_futures_orderbook <- function(value) {
  assert_class(value, "data.table")
  return(value)
}

assert_args_parse_futures_klines <- function(data) {
  if (!is.null(data)) {
    assert_list(data)
  }
  return(invisible(NULL))
}

assert_return_parse_futures_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "turnover"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["close"]])
  assert_double(value[["volume"]])
  assert_double(value[["turnover"]])
  return(value)
}

assert_args_KucoinFuturesTrading__initialize <- function(keys, base_url, async) {
  assert_list(keys)
  assert_scalar_character(base_url)
  assert_scalar_logical(async)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__initialize <- function(value) {
  assert_class(value, "KucoinFuturesTrading")
  return(value)
}

assert_args_KucoinFuturesTrading__add_order <- function(client_order_id, symbol, side, type, leverage, size, price, margin_mode, position_side, time_in_force, reduce_only, remark) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_character(type)
  assert_scalar_count(leverage)
  assert_between(leverage, lower = 1)
  assert_scalar_count(size)
  assert_between(size, lower = 1)
  if (!is.null(price)) {
    assert_scalar_character(price)
  }
  assert_scalar_character(margin_mode)
  assert_scalar_character(position_side)
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(reduce_only)) {
    assert_scalar_logical(reduce_only)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__add_order_test <- function(client_order_id, symbol, side, type, leverage, size, price, margin_mode, position_side, time_in_force, reduce_only, remark) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_scalar_character(type)
  assert_scalar_count(leverage)
  assert_between(leverage, lower = 1)
  assert_scalar_count(size)
  assert_between(size, lower = 1)
  if (!is.null(price)) {
    assert_scalar_character(price)
  }
  assert_scalar_character(margin_mode)
  assert_scalar_character(position_side)
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(reduce_only)) {
    assert_scalar_logical(reduce_only)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__add_order_test <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__add_order_batch <- function(orders) {
  assert_list(orders)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__add_order_batch <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__cancel_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__cancel_order_by_id <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesTrading__cancel_order_by_client_oid <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__cancel_order_by_client_oid <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesTrading__cancel_all <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__cancel_all <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesTrading__cancel_all_stop_orders <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__cancel_all_stop_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("cancelled_order_id"))
  assert_character(value[["cancelled_order_id"]])
  assert_no_missing_values(value[["cancelled_order_id"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_order_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "type", "side", "price", "size", "value", "deal_value", "deal_size", "leverage", "margin_mode", "position_side", "status", "created_at", "updated_at", "client_oid"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["value"]])
  assert_double(value[["deal_value"]])
  assert_double(value[["deal_size"]])
  assert_integer(value[["leverage"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_datetime(value[["updated_at"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_order_by_client_oid <- function(client_order_id) {
  assert_scalar_character(client_order_id)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_order_by_client_oid <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "type", "side", "price", "size", "value", "deal_value", "deal_size", "leverage", "margin_mode", "position_side", "status", "created_at", "updated_at", "client_oid"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["value"]])
  assert_double(value[["deal_value"]])
  assert_double(value[["deal_size"]])
  assert_integer(value[["leverage"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_datetime(value[["updated_at"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_order_list <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_order_list <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "type", "side", "price", "size", "value", "deal_value", "deal_size", "leverage", "margin_mode", "position_side", "status", "created_at", "updated_at", "client_oid"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["value"]])
  assert_double(value[["deal_value"]])
  assert_double(value[["deal_size"]])
  assert_integer(value[["leverage"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_datetime(value[["updated_at"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_recent_closed_orders <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_recent_closed_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "symbol", "type", "side", "price", "size", "value", "deal_value", "deal_size", "leverage", "margin_mode", "position_side", "status", "created_at", "updated_at", "client_oid"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["value"]])
  assert_double(value[["deal_value"]])
  assert_double(value[["deal_size"]])
  assert_integer(value[["leverage"]])
  assert_character(value[["margin_mode"]])
  assert_no_missing_values(value[["margin_mode"]])
  assert_character(value[["position_side"]])
  assert_no_missing_values(value[["position_side"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_datetime(value[["updated_at"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_stop_orders <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_stop_orders <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesTrading__get_fills <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_fills <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinFuturesTrading__get_recent_fills <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_recent_fills <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "trade_id", "order_id", "side", "liquidity", "force_taker", "price", "size", "value", "fee_rate", "fix_fee", "fee_currency", "fee", "order_type", "trade_type", "trade_time", "created_at"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["trade_id"]])
  assert_no_missing_values(value[["trade_id"]])
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["liquidity"]])
  assert_no_missing_values(value[["liquidity"]])
  assert_logical(value[["force_taker"]])
  assert_no_missing_values(value[["force_taker"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["value"]])
  assert_double(value[["fee_rate"]])
  assert_double(value[["fix_fee"]])
  assert_character(value[["fee_currency"]])
  assert_no_missing_values(value[["fee_currency"]])
  assert_double(value[["fee"]])
  assert_character(value[["order_type"]])
  assert_no_missing_values(value[["order_type"]])
  assert_character(value[["trade_type"]])
  assert_no_missing_values(value[["trade_type"]])
  assert_datetime(value[["trade_time"]])
  assert_no_missing_values(value[["trade_time"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_open_order_value <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_open_order_value <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("open_order_buy_size", "open_order_sell_size", "open_order_buy_cost", "open_order_sell_cost", "settle_currency"))
  assert_integer(value[["open_order_buy_size"]])
  assert_integer(value[["open_order_sell_size"]])
  assert_double(value[["open_order_buy_cost"]])
  assert_double(value[["open_order_sell_cost"]])
  assert_character(value[["settle_currency"]])
  assert_no_missing_values(value[["settle_currency"]])
  return(value)
}

assert_args_KucoinFuturesTrading__set_dcp <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__set_dcp <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("trade_type", "symbol", "system_time", "trigger_time"))
  assert_character(value[["trade_type"]])
  assert_no_missing_values(value[["trade_type"]])
  assert_character(value[["symbol"]])
  assert_double(value[["system_time"]])
  assert_no_missing_values(value[["system_time"]])
  assert_double(value[["trigger_time"]])
  assert_no_missing_values(value[["trigger_time"]])
  return(value)
}

assert_args_KucoinFuturesTrading__get_dcp <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinFuturesTrading__get_dcp <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("timeout", "symbols", "current_time"))
  assert_integer(value[["timeout"]])
  assert_character(value[["symbols"]])
  assert_no_missing_values(value[["symbols"]])
  assert_double(value[["current_time"]])
  assert_no_missing_values(value[["current_time"]])
  return(value)
}

assert_args_KucoinLending__get_loan_market <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinLending__get_loan_market <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinLending__get_loan_market_rate <- function(currency) {
  assert_scalar_character(currency)
  return(invisible(NULL))
}

assert_return_KucoinLending__get_loan_market_rate <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinLending__purchase <- function(currency, size, interest_rate) {
  assert_scalar_character(currency)
  assert_scalar_double(size)
  assert_scalar_double(interest_rate)
  return(invisible(NULL))
}

assert_return_KucoinLending__purchase <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_no"))
  assert_character(value[["order_no"]])
  assert_no_missing_values(value[["order_no"]])
  return(value)
}

assert_args_KucoinLending__modify_purchase <- function(currency, purchase_order_no, interest_rate) {
  assert_scalar_character(currency)
  assert_scalar_character(purchase_order_no)
  assert_scalar_double(interest_rate)
  return(invisible(NULL))
}

assert_return_KucoinLending__modify_purchase <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "purchase_order_no", "interest_rate", "status"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_character(value[["purchase_order_no"]])
  assert_no_missing_values(value[["purchase_order_no"]])
  assert_double(value[["interest_rate"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinLending__get_purchase_orders <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinLending__get_purchase_orders <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinLending__redeem <- function(currency, size, purchase_order_no) {
  assert_scalar_character(currency)
  assert_scalar_double(size)
  assert_scalar_character(purchase_order_no)
  return(invisible(NULL))
}

assert_return_KucoinLending__redeem <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_no"))
  assert_character(value[["order_no"]])
  assert_no_missing_values(value[["order_no"]])
  return(value)
}

assert_args_KucoinLending__get_redeem_orders <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinLending__get_redeem_orders <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginData__get_cross_margin_symbols <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginData__get_cross_margin_symbols <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinMarginData__get_isolated_margin_symbols <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinMarginData__get_margin_config <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "max_leverage", "warning_debt_ratio", "liq_debt_ratio"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_integer(value[["max_leverage"]])
  assert_double(value[["warning_debt_ratio"]])
  assert_double(value[["liq_debt_ratio"]])
  return(value)
}

assert_args_KucoinMarginData__get_collateral_ratio <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginData__get_collateral_ratio <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "lower_limit", "upper_limit", "collateral_ratio"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["lower_limit"]])
  assert_double(value[["upper_limit"]])
  assert_double(value[["collateral_ratio"]])
  return(value)
}

assert_args_KucoinMarginData__get_risk_limit <- function(is_isolated, query) {
  assert_scalar_logical(is_isolated)
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginData__get_risk_limit <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__open_short <- function(symbol, size, funds, type, price, is_isolated, client_order_id, stp, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size, dry_run) {
  assert_scalar_character(symbol)
  if (!is.null(size)) {
    assert_scalar_double(size)
  }
  if (!is.null(funds)) {
    assert_scalar_double(funds)
  }
  assert_scalar_character(type)
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  assert_scalar_logical(is_isolated)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_scalar_double(visible_size)
  }
  assert_scalar_logical(dry_run)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__open_short <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__close_short <- function(symbol, size, funds, type, price, is_isolated, client_order_id, stp, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size, dry_run) {
  assert_scalar_character(symbol)
  if (!is.null(size)) {
    assert_scalar_double(size)
  }
  if (!is.null(funds)) {
    assert_scalar_double(funds)
  }
  assert_scalar_character(type)
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  assert_scalar_logical(is_isolated)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_scalar_double(visible_size)
  }
  assert_scalar_logical(dry_run)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__close_short <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinMarginTrading__open_long <- function(symbol, size, funds, type, price, is_isolated, client_order_id, stp, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size, dry_run) {
  assert_scalar_character(symbol)
  if (!is.null(size)) {
    assert_scalar_double(size)
  }
  if (!is.null(funds)) {
    assert_scalar_double(funds)
  }
  assert_scalar_character(type)
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  assert_scalar_logical(is_isolated)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_scalar_double(visible_size)
  }
  assert_scalar_logical(dry_run)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__open_long <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinMarginTrading__close_long <- function(symbol, size, funds, type, price, is_isolated, client_order_id, stp, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size, dry_run) {
  assert_scalar_character(symbol)
  if (!is.null(size)) {
    assert_scalar_double(size)
  }
  if (!is.null(funds)) {
    assert_scalar_double(funds)
  }
  assert_scalar_character(type)
  if (!is.null(price)) {
    assert_scalar_double(price)
  }
  assert_scalar_logical(is_isolated)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_scalar_double(visible_size)
  }
  assert_scalar_logical(dry_run)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__close_long <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinMarginTrading__borrow <- function(currency, size, time_in_force, is_isolated, symbol, is_hf) {
  assert_scalar_character(currency)
  assert_scalar_double(size)
  assert_scalar_character(time_in_force)
  assert_scalar_logical(is_isolated)
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  assert_scalar_logical(is_hf)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__borrow <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_no", "actual_size"))
  assert_character(value[["order_no"]])
  assert_no_missing_values(value[["order_no"]])
  assert_double(value[["actual_size"]])
  return(value)
}

assert_args_KucoinMarginTrading__repay <- function(currency, size) {
  assert_scalar_character(currency)
  assert_scalar_double(size)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__repay <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__get_borrow_history <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__get_borrow_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__get_repay_history <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__get_repay_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__get_interest_history <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__get_interest_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarginTrading__get_borrow_rate <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinMarginTrading__get_borrow_rate <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinMarginTrading__modify_leverage <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("leverage", "status"))
  assert_double(value[["leverage"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinMarketData__get_announcements <- function(query, page_size, max_pages) {
  assert_list(query)
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_announcements <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("ann_id", "ann_title", "ann_type", "ann_desc", "c_time", "language", "ann_url"))
  assert_integer(value[["ann_id"]])
  assert_no_missing_values(value[["ann_id"]])
  assert_character(value[["ann_title"]])
  assert_character(value[["ann_type"]])
  assert_character(value[["ann_desc"]])
  assert_datetime(value[["c_time"]])
  assert_no_missing_values(value[["c_time"]])
  assert_character(value[["language"]])
  assert_no_missing_values(value[["language"]])
  assert_character(value[["ann_url"]])
  return(value)
}

assert_args_KucoinMarketData__get_currency <- function(currency, chain) {
  assert_scalar_character(currency)
  if (!is.null(chain)) {
    assert_scalar_character(chain)
  }
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_currency <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinMarketData__get_all_currencies <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinMarketData__get_symbol <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_symbol <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "name", "base_currency", "quote_currency", "fee_currency", "market", "base_min_size", "quote_min_size", "base_max_size", "quote_max_size", "base_increment", "quote_increment", "price_increment", "price_limit_rate", "min_funds", "is_margin_enabled", "enable_trading", "fee_category", "maker_fee_coefficient", "taker_fee_coefficient", "st", "callauction_is_enabled", "callauction_price_floor", "callauction_price_ceiling", "callauction_first_stage_start_time", "callauction_second_stage_start_time", "callauction_third_stage_start_time", "trading_start_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["name"]])
  assert_no_missing_values(value[["name"]])
  assert_character(value[["base_currency"]])
  assert_no_missing_values(value[["base_currency"]])
  assert_character(value[["quote_currency"]])
  assert_no_missing_values(value[["quote_currency"]])
  assert_character(value[["fee_currency"]])
  assert_no_missing_values(value[["fee_currency"]])
  assert_character(value[["market"]])
  assert_no_missing_values(value[["market"]])
  assert_double(value[["base_min_size"]])
  assert_double(value[["quote_min_size"]])
  assert_double(value[["base_max_size"]])
  assert_double(value[["quote_max_size"]])
  assert_double(value[["base_increment"]])
  assert_double(value[["quote_increment"]])
  assert_double(value[["price_increment"]])
  assert_double(value[["price_limit_rate"]])
  assert_double(value[["min_funds"]])
  assert_logical(value[["is_margin_enabled"]])
  assert_no_missing_values(value[["is_margin_enabled"]])
  assert_logical(value[["enable_trading"]])
  assert_no_missing_values(value[["enable_trading"]])
  assert_integer(value[["fee_category"]])
  assert_no_missing_values(value[["fee_category"]])
  assert_double(value[["maker_fee_coefficient"]])
  assert_double(value[["taker_fee_coefficient"]])
  assert_logical(value[["st"]])
  assert_no_missing_values(value[["st"]])
  assert_logical(value[["callauction_is_enabled"]])
  assert_no_missing_values(value[["callauction_is_enabled"]])
  assert_double(value[["callauction_price_floor"]])
  assert_double(value[["callauction_price_ceiling"]])
  assert_double(value[["callauction_first_stage_start_time"]])
  assert_double(value[["callauction_second_stage_start_time"]])
  assert_double(value[["callauction_third_stage_start_time"]])
  assert_double(value[["trading_start_time"]])
  return(value)
}

assert_args_KucoinMarketData__get_all_symbols <- function(market) {
  if (!is.null(market)) {
    assert_scalar_character(market)
  }
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_all_symbols <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "name", "base_currency", "quote_currency", "fee_currency", "market", "base_min_size", "quote_min_size", "base_max_size", "quote_max_size", "base_increment", "quote_increment", "price_increment", "price_limit_rate", "min_funds", "is_margin_enabled", "enable_trading", "fee_category", "maker_fee_coefficient", "taker_fee_coefficient", "st", "callauction_is_enabled", "callauction_price_floor", "callauction_price_ceiling", "callauction_first_stage_start_time", "callauction_second_stage_start_time", "callauction_third_stage_start_time", "trading_start_time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["name"]])
  assert_no_missing_values(value[["name"]])
  assert_character(value[["base_currency"]])
  assert_no_missing_values(value[["base_currency"]])
  assert_character(value[["quote_currency"]])
  assert_no_missing_values(value[["quote_currency"]])
  assert_character(value[["fee_currency"]])
  assert_no_missing_values(value[["fee_currency"]])
  assert_character(value[["market"]])
  assert_no_missing_values(value[["market"]])
  assert_double(value[["base_min_size"]])
  assert_double(value[["quote_min_size"]])
  assert_double(value[["base_max_size"]])
  assert_double(value[["quote_max_size"]])
  assert_double(value[["base_increment"]])
  assert_double(value[["quote_increment"]])
  assert_double(value[["price_increment"]])
  assert_double(value[["price_limit_rate"]])
  assert_double(value[["min_funds"]])
  assert_logical(value[["is_margin_enabled"]])
  assert_no_missing_values(value[["is_margin_enabled"]])
  assert_logical(value[["enable_trading"]])
  assert_no_missing_values(value[["enable_trading"]])
  assert_integer(value[["fee_category"]])
  assert_no_missing_values(value[["fee_category"]])
  assert_double(value[["maker_fee_coefficient"]])
  assert_double(value[["taker_fee_coefficient"]])
  assert_logical(value[["st"]])
  assert_no_missing_values(value[["st"]])
  assert_logical(value[["callauction_is_enabled"]])
  assert_no_missing_values(value[["callauction_is_enabled"]])
  assert_double(value[["callauction_price_floor"]])
  assert_double(value[["callauction_price_ceiling"]])
  assert_double(value[["callauction_first_stage_start_time"]])
  assert_double(value[["callauction_second_stage_start_time"]])
  assert_double(value[["callauction_third_stage_start_time"]])
  assert_double(value[["trading_start_time"]])
  return(value)
}

assert_args_KucoinMarketData__get_ticker <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_ticker <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("time", "sequence", "price", "size", "best_bid", "best_bid_size", "best_ask", "best_ask_size"))
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_double(value[["best_bid"]])
  assert_double(value[["best_bid_size"]])
  assert_double(value[["best_ask"]])
  assert_double(value[["best_ask_size"]])
  return(value)
}

assert_return_KucoinMarketData__get_all_tickers <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "symbol_name", "buy", "sell", "change_rate", "change_price", "high", "low", "vol", "vol_value", "last", "average_price", "taker_fee_rate", "maker_fee_rate", "time"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["symbol_name"]])
  assert_no_missing_values(value[["symbol_name"]])
  assert_double(value[["buy"]])
  assert_double(value[["sell"]])
  assert_double(value[["change_rate"]])
  assert_double(value[["change_price"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["vol"]])
  assert_double(value[["vol_value"]])
  assert_double(value[["last"]])
  assert_double(value[["average_price"]])
  assert_double(value[["taker_fee_rate"]])
  assert_double(value[["maker_fee_rate"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  return(value)
}

assert_args_KucoinMarketData__get_trade_history <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_trade_history <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("sequence", "side", "price", "size", "time", "trade_id"))
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["trade_id"]])
  assert_no_missing_values(value[["trade_id"]])
  return(value)
}

assert_args_KucoinMarketData__get_part_orderbook <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_part_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("time", "sequence", "side", "level", "price", "size"))
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_count(value[["level"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  return(value)
}

assert_args_KucoinMarketData__get_full_orderbook <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_full_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("time", "sequence", "side", "level", "price", "size"))
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_count(value[["level"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  return(value)
}

assert_args_KucoinMarketData__get_24hr_stats <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_24hr_stats <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("time", "symbol", "buy", "sell", "change_rate", "change_price", "high", "low", "vol", "vol_value", "last", "average_price", "taker_fee_rate", "maker_fee_rate", "taker_coefficient", "maker_coefficient"))
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_double(value[["buy"]])
  assert_double(value[["sell"]])
  assert_double(value[["change_rate"]])
  assert_double(value[["change_price"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["vol"]])
  assert_double(value[["vol_value"]])
  assert_double(value[["last"]])
  assert_double(value[["average_price"]])
  assert_double(value[["taker_fee_rate"]])
  assert_double(value[["maker_fee_rate"]])
  assert_double(value[["taker_coefficient"]])
  assert_double(value[["maker_coefficient"]])
  return(value)
}

assert_return_KucoinMarketData__get_market_list <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("market"))
  assert_character(value[["market"]])
  assert_no_missing_values(value[["market"]])
  return(value)
}

assert_args_KucoinMarketData__get_klines <- function(symbol, timeframe) {
  assert_scalar_character(symbol)
  assert_scalar_character(timeframe)
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "turnover"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["close"]])
  assert_double(value[["volume"]])
  assert_double(value[["turnover"]])
  return(value)
}

assert_return_KucoinMarketData__get_server_time <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("server_time", "datetime"))
  assert_double(value[["server_time"]])
  assert_no_missing_values(value[["server_time"]])
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  return(value)
}

assert_return_KucoinMarketData__get_service_status <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("status", "msg"))
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["msg"]])
  assert_no_missing_values(value[["msg"]])
  return(value)
}

assert_args_KucoinMarketData__get_fiat_prices <- function(base, currencies) {
  if (!is.null(base)) {
    assert_scalar_character(base)
  }
  if (!is.null(currencies)) {
    assert_scalar_character(currencies)
  }
  return(invisible(NULL))
}

assert_return_KucoinMarketData__get_fiat_prices <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinOcoOrders__add_order <- function(symbol, side, price, size, stop_price, limit_price, client_order_id, remark, trade_type) {
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_any_of(
    price,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  assert_any_of(
    size,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  assert_any_of(
    stop_price,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  assert_any_of(
    limit_price,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  assert_scalar_character(trade_type)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinOcoOrders__cancel_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__cancel_order_by_id <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinOcoOrders__cancel_order_by_client_oid <- function(client_order_id) {
  assert_scalar_character(client_order_id)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__cancel_order_by_client_oid <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinOcoOrders__cancel_all <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__cancel_all <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinOcoOrders__get_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__get_order_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "symbol", "client_oid", "order_time", "status"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["client_oid"]])
  assert_datetime(value[["order_time"]])
  assert_no_missing_values(value[["order_time"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinOcoOrders__get_order_by_client_oid <- function(client_order_id) {
  assert_scalar_character(client_order_id)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__get_order_by_client_oid <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "symbol", "client_oid", "order_time", "status"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["client_oid"]])
  assert_datetime(value[["order_time"]])
  assert_no_missing_values(value[["order_time"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinOcoOrders__get_order_detail_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__get_order_detail_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "symbol", "client_oid", "order_time", "status", "sub_order_id", "sub_order_symbol", "sub_order_side", "sub_order_price", "sub_order_size", "sub_order_status", "sub_order_stop_price"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["client_oid"]])
  assert_datetime(value[["order_time"]])
  assert_no_missing_values(value[["order_time"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["sub_order_id"]])
  assert_no_missing_values(value[["sub_order_id"]])
  assert_character(value[["sub_order_symbol"]])
  assert_no_missing_values(value[["sub_order_symbol"]])
  assert_character(value[["sub_order_side"]])
  assert_no_missing_values(value[["sub_order_side"]])
  assert_double(value[["sub_order_price"]])
  assert_double(value[["sub_order_size"]])
  assert_character(value[["sub_order_status"]])
  assert_no_missing_values(value[["sub_order_status"]])
  assert_double(value[["sub_order_stop_price"]])
  return(value)
}

assert_args_KucoinOcoOrders__get_order_list <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinOcoOrders__get_order_list <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinStopOrders__add_order <- function(type, symbol, side, stop_price, client_order_id, price, size, funds, stp, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size, trade_type) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_any_of(
    stop_price,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(price)) {
    assert_any_of(
      price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(size)) {
    assert_any_of(
      size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(funds)) {
    assert_any_of(
      funds,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_any_of(
      visible_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  assert_scalar_character(trade_type)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  return(value)
}

assert_args_KucoinStopOrders__cancel_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__cancel_order_by_id <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinStopOrders__cancel_order_by_client_oid <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__cancel_order_by_client_oid <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("cancelled_order_id", "client_oid"))
  assert_character(value[["cancelled_order_id"]])
  assert_no_missing_values(value[["cancelled_order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinStopOrders__cancel_all <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__cancel_all <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinStopOrders__get_order_by_id <- function(order_id) {
  assert_scalar_character(order_id)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__get_order_by_id <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinStopOrders__get_order_by_client_oid <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__get_order_by_client_oid <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinStopOrders__get_order_list <- function(query) {
  assert_list(query)
  return(invisible(NULL))
}

assert_return_KucoinStopOrders__get_order_list <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinSubAccount__add_sub_account <- function(password, sub_name, access, remarks) {
  assert_scalar_character(password)
  assert_scalar_character(sub_name)
  assert_scalar_character(access)
  if (!is.null(remarks)) {
    assert_scalar_character(remarks)
  }
  return(invisible(NULL))
}

assert_return_KucoinSubAccount__add_sub_account <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("uid", "sub_name", "remarks", "access"))
  assert_integer(value[["uid"]])
  assert_no_missing_values(value[["uid"]])
  assert_character(value[["sub_name"]])
  assert_no_missing_values(value[["sub_name"]])
  assert_character(value[["remarks"]])
  assert_character(value[["access"]])
  assert_no_missing_values(value[["access"]])
  return(value)
}

assert_args_KucoinSubAccount__get_sub_account_list <- function(page_size, max_pages) {
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinSubAccount__get_sub_account_list <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinSubAccount__get_detail_balance <- function(sub_user_id, include_base_amount) {
  assert_scalar_character(sub_user_id)
  assert_scalar_logical(include_base_amount)
  return(invisible(NULL))
}

assert_return_KucoinSubAccount__get_detail_balance <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinSubAccount__get_all_spot_balances <- function(page_size, max_pages) {
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinSubAccount__get_all_spot_balances <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinTrading__add_order <- function(type, symbol, side, client_order_id, price, size, funds, stp, tags, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(price)) {
    assert_any_of(
      price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(size)) {
    assert_any_of(
      size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(funds)) {
    assert_any_of(
      funds,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(tags)) {
    assert_scalar_character(tags)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_any_of(
      visible_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__add_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinTrading__add_order_test <- function(type, symbol, side, client_order_id, price, size, funds, stp, tags, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(price)) {
    assert_any_of(
      price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(size)) {
    assert_any_of(
      size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(funds)) {
    assert_any_of(
      funds,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(tags)) {
    assert_scalar_character(tags)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_any_of(
      visible_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__add_order_test <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinTrading__add_order_batch <- function(order_list) {
  assert_list(order_list)
  return(invisible(NULL))
}

assert_return_KucoinTrading__add_order_batch <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid", "success", "fail_msg"))
  assert_character(value[["order_id"]])
  assert_character(value[["client_oid"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  assert_character(value[["fail_msg"]])
  return(value)
}

assert_args_KucoinTrading__cancel_order_by_id <- function(order_id, symbol) {
  assert_scalar_character(order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_order_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  return(value)
}

assert_args_KucoinTrading__cancel_order_by_client_oid <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_order_by_client_oid <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("client_oid"))
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinTrading__cancel_partial_order <- function(order_id, symbol, cancel_size) {
  assert_scalar_character(order_id)
  assert_scalar_character(symbol)
  assert_any_of(
    cancel_size,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_character(.x)
    }
  )
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_partial_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "cancel_size"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["cancel_size"]])
  return(value)
}

assert_args_KucoinTrading__cancel_all_by_symbol <- function(symbol) {
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_all_by_symbol <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("result"))
  assert_character(value[["result"]])
  assert_no_missing_values(value[["result"]])
  return(value)
}

assert_return_KucoinTrading__cancel_all <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbol", "status"))
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinTrading__get_order_by_id <- function(order_id, symbol) {
  assert_scalar_character(order_id)
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__get_order_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "symbol", "side", "type", "price", "size", "created_at", "last_updated_at"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_character(value[["type"]])
  assert_no_missing_values(value[["type"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_datetime(value[["last_updated_at"]])
  assert_no_missing_values(value[["last_updated_at"]])
  return(value)
}

assert_args_KucoinTrading__get_order_by_client_oid <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__get_order_by_client_oid <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("client_oid", "symbol", "side", "created_at"))
  assert_character(value[["client_oid"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  return(value)
}

assert_args_KucoinTrading__get_fills <- function(symbol, order_id, side, type, last_id, limit, start_at, end_at) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(order_id)) {
    assert_scalar_character(order_id)
  }
  if (!is.null(side)) {
    assert_scalar_character(side)
  }
  if (!is.null(type)) {
    assert_scalar_character(type)
  }
  if (!is.null(last_id)) {
    assert_scalar_character(last_id)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(start_at)) {
    assert_scalar_double(start_at)
  }
  if (!is.null(end_at)) {
    assert_scalar_double(end_at)
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__get_fills <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_return_KucoinTrading__get_symbols_with_open_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("symbols"))
  assert_character(value[["symbols"]])
  assert_no_missing_values(value[["symbols"]])
  return(value)
}

assert_args_KucoinTrading__get_open_orders <- function(symbol) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__get_open_orders <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinTrading__get_closed_orders <- function(symbol, side, type, start_at, end_at, limit, last_id) {
  if (!is.null(symbol)) {
    assert_scalar_character(symbol)
  }
  if (!is.null(side)) {
    assert_scalar_character(side)
  }
  if (!is.null(type)) {
    assert_scalar_character(type)
  }
  if (!is.null(start_at)) {
    assert_scalar_double(start_at)
  }
  if (!is.null(end_at)) {
    assert_scalar_double(end_at)
  }
  if (!is.null(limit)) {
    assert_scalar_count(limit)
  }
  if (!is.null(last_id)) {
    assert_scalar_character(last_id)
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__get_closed_orders <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "symbol", "side", "created_at"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_character(value[["symbol"]])
  assert_no_missing_values(value[["symbol"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  return(value)
}

assert_args_KucoinTrading__add_order_sync <- function(type, symbol, side, client_order_id, price, size, funds, stp, tags, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size) {
  assert_scalar_character(type)
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(price)) {
    assert_any_of(
      price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(size)) {
    assert_any_of(
      size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(funds)) {
    assert_any_of(
      funds,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(tags)) {
    assert_scalar_character(tags)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_any_of(
      visible_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__add_order_sync <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinTrading__add_order_batch_sync <- function(order_list) {
  assert_list(order_list)
  return(invisible(NULL))
}

assert_return_KucoinTrading__add_order_batch_sync <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "client_oid", "success", "status", "deal_size", "remain_size", "canceled_size", "fail_msg"))
  assert_character(value[["order_id"]])
  assert_character(value[["client_oid"]])
  assert_logical(value[["success"]])
  assert_no_missing_values(value[["success"]])
  assert_character(value[["status"]])
  assert_double(value[["deal_size"]])
  assert_double(value[["remain_size"]])
  assert_double(value[["canceled_size"]])
  assert_character(value[["fail_msg"]])
  return(value)
}

assert_args_KucoinTrading__cancel_order_by_id_sync <- function(order_id, symbol) {
  assert_scalar_character(order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_order_by_id_sync <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id", "origin_size", "deal_size", "remain_size", "canceled_size", "status"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  assert_double(value[["origin_size"]])
  assert_double(value[["deal_size"]])
  assert_double(value[["remain_size"]])
  assert_double(value[["canceled_size"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinTrading__cancel_order_by_client_oid_sync <- function(client_order_id, symbol) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(symbol)
  return(invisible(NULL))
}

assert_return_KucoinTrading__cancel_order_by_client_oid_sync <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("client_oid", "origin_size", "deal_size", "remain_size", "canceled_size", "status"))
  assert_character(value[["client_oid"]])
  assert_double(value[["origin_size"]])
  assert_double(value[["deal_size"]])
  assert_double(value[["remain_size"]])
  assert_double(value[["canceled_size"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  return(value)
}

assert_args_KucoinTrading__modify_order <- function(symbol, order_id, client_order_id, new_price, new_size) {
  assert_scalar_character(symbol)
  if (!is.null(order_id)) {
    assert_scalar_character(order_id)
  }
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(new_price)) {
    assert_any_of(
      new_price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(new_size)) {
    assert_any_of(
      new_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__modify_order <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("new_order_id", "client_oid"))
  assert_character(value[["new_order_id"]])
  assert_no_missing_values(value[["new_order_id"]])
  assert_character(value[["client_oid"]])
  return(value)
}

assert_args_KucoinTrading__set_dcp <- function(symbols) {
  if (!is.null(symbols)) {
    assert_scalar_character(symbols)
  }
  return(invisible(NULL))
}

assert_return_KucoinTrading__set_dcp <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("current_time", "trigger_time"))
  assert_integer(value[["current_time"]])
  assert_no_missing_values(value[["current_time"]])
  assert_integer(value[["trigger_time"]])
  assert_no_missing_values(value[["trigger_time"]])
  return(value)
}

assert_return_KucoinTrading__get_dcp <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinTransfer__add_transfer <- function(client_order_id, currency, amount, type, from_account_type, to_account_type, from_user_id, from_account_tag, to_user_id, to_account_tag) {
  assert_scalar_character(client_order_id)
  assert_scalar_character(currency)
  assert_scalar_character(amount)
  assert_scalar_character(type)
  assert_scalar_character(from_account_type)
  assert_scalar_character(to_account_type)
  if (!is.null(from_user_id)) {
    assert_scalar_character(from_user_id)
  }
  if (!is.null(from_account_tag)) {
    assert_scalar_character(from_account_tag)
  }
  if (!is.null(to_user_id)) {
    assert_scalar_character(to_user_id)
  }
  if (!is.null(to_account_tag)) {
    assert_scalar_character(to_account_tag)
  }
  return(invisible(NULL))
}

assert_return_KucoinTransfer__add_transfer <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("order_id"))
  assert_character(value[["order_id"]])
  assert_no_missing_values(value[["order_id"]])
  return(value)
}

assert_args_KucoinTransfer__get_transferable <- function(currency, type, tag) {
  assert_scalar_character(currency)
  assert_scalar_character(type)
  if (!is.null(tag)) {
    assert_scalar_character(tag)
  }
  return(invisible(NULL))
}

assert_return_KucoinTransfer__get_transferable <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "balance", "available", "holds", "transferable"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_double(value[["balance"]])
  assert_double(value[["available"]])
  assert_double(value[["holds"]])
  assert_double(value[["transferable"]])
  return(value)
}

assert_args_KucoinWithdrawal__add_withdrawal <- function(currency, to_address, amount, withdraw_type, chain, memo, is_inner, remark, fee_deduct_type) {
  assert_scalar_character(currency)
  assert_scalar_character(to_address)
  assert_scalar_character(amount)
  assert_scalar_character(withdraw_type)
  if (!is.null(chain)) {
    assert_scalar_character(chain)
  }
  if (!is.null(memo)) {
    assert_scalar_character(memo)
  }
  if (!is.null(is_inner)) {
    assert_scalar_logical(is_inner)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(fee_deduct_type)) {
    assert_scalar_character(fee_deduct_type)
  }
  return(invisible(NULL))
}

assert_return_KucoinWithdrawal__add_withdrawal <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("withdrawal_id"))
  assert_character(value[["withdrawal_id"]])
  assert_no_missing_values(value[["withdrawal_id"]])
  return(value)
}

assert_args_KucoinWithdrawal__cancel_withdrawal <- function(withdrawal_id) {
  assert_scalar_character(withdrawal_id)
  return(invisible(NULL))
}

assert_return_KucoinWithdrawal__cancel_withdrawal <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("withdrawal_id"))
  assert_character(value[["withdrawal_id"]])
  assert_no_missing_values(value[["withdrawal_id"]])
  return(value)
}

assert_args_KucoinWithdrawal__get_withdrawal_quotas <- function(currency, chain) {
  assert_scalar_character(currency)
  if (!is.null(chain)) {
    assert_scalar_character(chain)
  }
  return(invisible(NULL))
}

assert_return_KucoinWithdrawal__get_withdrawal_quotas <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("currency", "chain", "is_withdraw_enabled", "available_amount", "remain_amount", "withdraw_min_fee", "inner_withdraw_min_fee", "withdraw_min_size", "precision", "limit_btc_amount", "used_btc_amount", "locked_amount", "quota_currency", "limit_quota_currency_amount", "used_quota_currency_amount", "reason"))
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_character(value[["chain"]])
  assert_no_missing_values(value[["chain"]])
  assert_logical(value[["is_withdraw_enabled"]])
  assert_double(value[["available_amount"]])
  assert_double(value[["remain_amount"]])
  assert_double(value[["withdraw_min_fee"]])
  assert_double(value[["inner_withdraw_min_fee"]])
  assert_double(value[["withdraw_min_size"]])
  assert_integer(value[["precision"]])
  assert_double(value[["limit_btc_amount"]])
  assert_double(value[["used_btc_amount"]])
  assert_double(value[["locked_amount"]])
  assert_character(value[["quota_currency"]])
  assert_no_missing_values(value[["quota_currency"]])
  assert_double(value[["limit_quota_currency_amount"]])
  assert_double(value[["used_quota_currency_amount"]])
  assert_character(value[["reason"]])
  return(value)
}

assert_args_KucoinWithdrawal__get_withdrawal_history <- function(currency, status, start_at, end_at, page_size, max_pages) {
  if (!is.null(currency)) {
    assert_scalar_character(currency)
  }
  if (!is.null(status)) {
    assert_scalar_character(status)
  }
  if (!is.null(start_at)) {
    assert_scalar_double(start_at)
  }
  if (!is.null(end_at)) {
    assert_scalar_double(end_at)
  }
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  return(invisible(NULL))
}

assert_return_KucoinWithdrawal__get_withdrawal_history <- function(value) {
  assert_data_table(value)
  return(value)
}

assert_args_KucoinWithdrawal__get_withdrawal_by_id <- function(withdrawal_id) {
  assert_scalar_character(withdrawal_id)
  return(invisible(NULL))
}

assert_return_KucoinWithdrawal__get_withdrawal_by_id <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("id", "currency", "chain_id", "chain_name", "status", "address", "memo", "is_inner", "amount", "fee", "wallet_tx_id", "created_at", "cancel_type", "uid", "currency_name", "failure_reason", "failure_reason_msg", "address_remark", "remark", "taxes", "tax_description", "tx_id", "return_status", "return_amount", "return_currency"))
  assert_character(value[["id"]])
  assert_no_missing_values(value[["id"]])
  assert_character(value[["currency"]])
  assert_no_missing_values(value[["currency"]])
  assert_character(value[["chain_id"]])
  assert_character(value[["chain_name"]])
  assert_character(value[["status"]])
  assert_no_missing_values(value[["status"]])
  assert_character(value[["address"]])
  assert_no_missing_values(value[["address"]])
  assert_character(value[["memo"]])
  assert_no_missing_values(value[["memo"]])
  assert_logical(value[["is_inner"]])
  assert_no_missing_values(value[["is_inner"]])
  assert_double(value[["amount"]])
  assert_double(value[["fee"]])
  assert_character(value[["wallet_tx_id"]])
  assert_datetime(value[["created_at"]])
  assert_no_missing_values(value[["created_at"]])
  assert_character(value[["cancel_type"]])
  assert_no_missing_values(value[["cancel_type"]])
  assert_integer(value[["uid"]])
  assert_no_missing_values(value[["uid"]])
  assert_character(value[["currency_name"]])
  assert_no_missing_values(value[["currency_name"]])
  assert_character(value[["failure_reason"]])
  assert_character(value[["failure_reason_msg"]])
  assert_character(value[["address_remark"]])
  assert_character(value[["remark"]])
  assert_double(value[["taxes"]])
  assert_character(value[["tax_description"]])
  assert_character(value[["tx_id"]])
  assert_character(value[["return_status"]])
  assert_no_missing_values(value[["return_status"]])
  assert_double(value[["return_amount"]])
  assert_character(value[["return_currency"]])
  assert_no_missing_values(value[["return_currency"]])
  return(value)
}

assert_args_kucoin_backfill_klines <- function(timeframes, file, base_url, sleep, verbose) {
  assert_character(timeframes)
  assert_no_missing_values(timeframes)
  assert_minimum_length(timeframes, 1L)
  assert_scalar_character(file)
  assert_scalar_character(base_url)
  assert_scalar_double(sleep)
  assert_between(sleep, lower = 0, upper = Inf, upper_inclusive = FALSE)
  assert_scalar_logical(verbose)
  return(invisible(NULL))
}

assert_return_kucoin_backfill_klines <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_ns_to_datetime <- function(ns) {
  return(invisible(NULL))
}

assert_return_ns_to_datetime <- function(value) {
  assert_class(value, "POSIXct")
  return(value)
}

assert_args_collapse_string_array_fields <- function(x, fields) {
  assert_list(x)
  assert_character(fields)
  assert_no_missing_values(fields)
  return(invisible(NULL))
}

assert_return_collapse_string_array_fields <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_coerce_cols <- function(dt, cols, fn) {
  assert_class(dt, "data.table")
  assert_character(cols)
  assert_no_missing_values(cols)
  assert_function(fn)
  return(invisible(NULL))
}

assert_return_coerce_cols <- function(value) {
  assert_class(value, "data.table")
  return(value)
}

assert_args_parse_orderbook <- function(data) {
  assert_list(data)
  return(invisible(NULL))
}

assert_return_parse_orderbook <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("time", "sequence", "side", "level", "price", "size"))
  assert_datetime(value[["time"]])
  assert_no_missing_values(value[["time"]])
  assert_character(value[["sequence"]])
  assert_no_missing_values(value[["sequence"]])
  assert_character(value[["side"]])
  assert_no_missing_values(value[["side"]])
  assert_count(value[["level"]])
  assert_double(value[["price"]])
  assert_double(value[["size"]])
  return(value)
}

assert_args_parse_klines <- function(data) {
  if (!is.null(data)) {
    assert_list(data)
  }
  return(invisible(NULL))
}

assert_return_parse_klines <- function(value) {
  assert_data_table(value)
  assert_has_columns(value, c("datetime", "open", "high", "low", "close", "volume", "turnover"))
  assert_datetime(value[["datetime"]])
  assert_no_missing_values(value[["datetime"]])
  assert_double(value[["open"]])
  assert_double(value[["high"]])
  assert_double(value[["low"]])
  assert_double(value[["close"]])
  assert_double(value[["volume"]])
  assert_double(value[["turnover"]])
  return(value)
}

assert_args_flatten_pages <- function(pages) {
  assert_list(pages)
  return(invisible(NULL))
}

assert_return_flatten_pages <- function(value) {
  assert_class(value, "data.table")
  return(value)
}

assert_args_sign_request <- function(req, keys, method, path, body, .get_timestamp_ms) {
  assert_class(req, "httr2_request")
  assert_list(keys)
  assert_has_names(keys, c("api_key", "api_secret", "api_passphrase", "key_version"))
  assert_character(keys[["api_key"]])
  assert_no_missing_values(keys[["api_key"]])
  assert_character(keys[["api_secret"]])
  assert_no_missing_values(keys[["api_secret"]])
  assert_character(keys[["api_passphrase"]])
  assert_no_missing_values(keys[["api_passphrase"]])
  assert_character(keys[["key_version"]])
  assert_no_missing_values(keys[["key_version"]])
  assert_scalar_character(method)
  assert_scalar_character(path)
  assert_scalar_character(body)
  if (!is.null(.get_timestamp_ms)) {
    assert_function(.get_timestamp_ms)
  }
  return(invisible(NULL))
}

assert_return_sign_request <- function(value) {
  assert_class(value, "httr2_request")
  return(value)
}

assert_args_kucoin_serialize_body <- function(body) {
  if (!is.null(body)) {
    assert_list(body)
  }
  return(invisible(NULL))
}

assert_return_kucoin_serialize_body <- function(value) {
  if (!is.null(value)) {
    assert_scalar_character(value)
  }
  return(value)
}

assert_args_kucoin_sign_req <- function(req, keys, ctx) {
  assert_class(req, "httr2_request")
  assert_list(keys)
  assert_has_names(keys, c("api_key", "api_secret", "api_passphrase", "key_version"))
  assert_character(keys[["api_key"]])
  assert_no_missing_values(keys[["api_key"]])
  assert_character(keys[["api_secret"]])
  assert_no_missing_values(keys[["api_secret"]])
  assert_character(keys[["api_passphrase"]])
  assert_no_missing_values(keys[["api_passphrase"]])
  assert_character(keys[["key_version"]])
  assert_no_missing_values(keys[["key_version"]])
  assert_list(ctx)
  return(invisible(NULL))
}

assert_return_kucoin_sign_req <- function(value) {
  assert_class(value, "httr2_request")
  return(value)
}

assert_args_parse_kucoin_response <- function(resp) {
  assert_class(resp, "httr2_response")
  return(invisible(NULL))
}

assert_return_parse_kucoin_response <- function(value) {
  return(value)
}

assert_args_kucoin_paginate <- function(base_url, endpoint, method, query, body, keys, sign, parse_envelope, .perform, .parser, is_async, page_size, max_pages, items_field, timeout, .get_timestamp_ms) {
  assert_scalar_character(base_url)
  assert_scalar_character(endpoint)
  assert_scalar_character(method)
  assert_list(query)
  if (!is.null(body)) {
    assert_list(body)
  }
  if (!is.null(keys)) {
    assert_list(keys)
    assert_has_names(keys, c("api_key", "api_secret", "api_passphrase", "key_version"))
    assert_character(keys[["api_key"]])
    assert_no_missing_values(keys[["api_key"]])
    assert_character(keys[["api_secret"]])
    assert_no_missing_values(keys[["api_secret"]])
    assert_character(keys[["api_passphrase"]])
    assert_no_missing_values(keys[["api_passphrase"]])
    assert_character(keys[["key_version"]])
    assert_no_missing_values(keys[["key_version"]])
  }
  if (!is.null(sign)) {
    assert_function(sign)
  }
  assert_function(parse_envelope)
  assert_function(.perform)
  assert_function(.parser)
  assert_scalar_logical(is_async)
  assert_scalar_count(page_size)
  assert_between(page_size, lower = 1)
  assert_scalar_double(max_pages)
  assert_between(max_pages, lower = 1)
  assert_scalar_character(items_field)
  assert_scalar_double(timeout)
  assert_between(timeout, lower = 0, lower_inclusive = FALSE, upper = Inf, upper_inclusive = FALSE)
  if (!is.null(.get_timestamp_ms)) {
    assert_function(.get_timestamp_ms)
  }
  return(invisible(NULL))
}

assert_return_kucoin_paginate <- function(value) {
  return(value)
}

assert_args_verify_symbol <- function(ticker) {
  assert_scalar_character(ticker)
  return(invisible(NULL))
}

assert_return_verify_symbol <- function(value) {
  assert_scalar_logical(value)
  return(value)
}

assert_args_validate_order_params <- function(type, symbol, side, client_order_id, price, size, funds, stp, tags, remark, time_in_force, cancel_after, post_only, hidden, iceberg, visible_size) {
  assert_scalar_character(type)
  assert_value_in_set(type, c("limit", "market"))
  assert_scalar_character(symbol)
  assert_scalar_character(side)
  assert_value_in_set(side, c("buy", "sell"))
  if (!is.null(client_order_id)) {
    assert_scalar_character(client_order_id)
  }
  if (!is.null(price)) {
    assert_any_of(
      price,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(size)) {
    assert_any_of(
      size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(funds)) {
    assert_any_of(
      funds,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  if (!is.null(stp)) {
    assert_scalar_character(stp)
  }
  if (!is.null(tags)) {
    assert_scalar_character(tags)
  }
  if (!is.null(remark)) {
    assert_scalar_character(remark)
  }
  if (!is.null(time_in_force)) {
    assert_scalar_character(time_in_force)
  }
  if (!is.null(cancel_after)) {
    assert_scalar_double(cancel_after)
  }
  if (!is.null(post_only)) {
    assert_scalar_logical(post_only)
  }
  if (!is.null(hidden)) {
    assert_scalar_logical(hidden)
  }
  if (!is.null(iceberg)) {
    assert_scalar_logical(iceberg)
  }
  if (!is.null(visible_size)) {
    assert_any_of(
      visible_size,
      function(.x) {
        assert_scalar_double(.x)
      },
      function(.x) {
        assert_scalar_character(.x)
      }
    )
  }
  return(invisible(NULL))
}

assert_return_validate_order_params <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_validate_margin_order_params <- function(is_isolated, auto_borrow, auto_repay) {
  if (!is.null(is_isolated)) {
    assert_scalar_logical(is_isolated)
  }
  if (!is.null(auto_borrow)) {
    assert_scalar_logical(auto_borrow)
  }
  if (!is.null(auto_repay)) {
    assert_scalar_logical(auto_repay)
  }
  return(invisible(NULL))
}

assert_return_validate_margin_order_params <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_validate_batch_order <- function(order) {
  assert_list(order)
  return(invisible(NULL))
}

assert_return_validate_batch_order <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_get_base_url <- function(url) {
  if (!is.null(url)) {
    assert_scalar_character(url)
  }
  return(invisible(NULL))
}

assert_return_get_base_url <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_get_futures_base_url <- function(url) {
  if (!is.null(url)) {
    assert_scalar_character(url)
  }
  return(invisible(NULL))
}

assert_return_get_futures_base_url <- function(value) {
  assert_scalar_character(value)
  return(value)
}

assert_args_get_api_keys <- function(api_key, api_secret, api_passphrase, key_version) {
  assert_scalar_character(api_key)
  assert_scalar_character(api_secret)
  assert_scalar_character(api_passphrase)
  assert_scalar_character(key_version)
  return(invisible(NULL))
}

assert_return_get_api_keys <- function(value) {
  assert_list(value)
  return(value)
}

assert_args_get_sub_account <- function(sub_account_name, sub_account_password) {
  assert_scalar_character(sub_account_name)
  assert_scalar_character(sub_account_password)
  return(invisible(NULL))
}

assert_return_get_sub_account <- function(value) {
  assert_list(value)
  return(value)
}

assert_return_time_convert_from_kucoin <- function(value) {
  assert_class(value, "POSIXct")
  return(value)
}

assert_return_time_convert_to_kucoin <- function(value) {
  assert_any_of(
    value,
    function(.x) {
      assert_scalar_double(.x)
    },
    function(.x) {
      assert_scalar_integer(.x)
    }
  )
  return(value)
}

